<?php

$isActive = false;

if($isActive) {
    echo "is active";
} else {
    echo "is not active";
}

$isActive = !$isActive;

if($isActive) {
    echo "is active";
} else {
    echo "is not active";
}
